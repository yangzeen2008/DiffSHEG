# 决策日志

> 记录会影响训练、推理、统计或论文表述的选择。候选结论不能伪装成已冻结决策。

| ID | 日期 | 决策 | 当前状态 | 主要依据 | 重新评估条件 |
|---|---|---|---|---|---|
| D-001 | 2026-08-30 | SO(3) 释放长度暂不修改默认值 7；`blend=5` 仅作为候选 | 候选，未冻结 | [R-2026-08-30-001](records/2026-08-30-transition-blend-validation.md)：三段中 5 帧更接近 GT 动态，7 帧最稳但偏平滑 | 完成全验证集关节级统计和 `blend5/7` 去标签盲评 |
| D-002 | 2026-08-30 | `beat_FM_aa_x0_aligned_v1` 作为当前有效主实验；旧 HuBERT/动作缓存不对齐的实验只用于历史趋势 | **已废弃** | 后续跨模态复核发现 E-007 的 120 FPS 动作和 60 FPS 表情仍被按 15 FPS 索引 | 由 D-004 替代 |
| D-003 | 2026-08-30 | 最终默认值只在 `blend=5/7` 的全验证集独立顺序推理和去标签盲评后冻结 | 评估中 | [R-2026-08-30-002](records/2026-08-30-full-validation-protocol.md) | 100 窗口预检通过后执行全量；按预注册的安全、保真与动态规则选择 |
| D-004 | 2026-08-30 | 正式模型必须使用 `beat_4english_15_141_sync_v1`：动作/表情统一 15 FPS、音频 16 kHz，运动 v3 与 HuBERT v3 必须绑定时间轴 manifest/cache ID；E-007 不再是主模型 | 已冻结 | [R-2026-08-30-003](records/2026-08-30-cross-modal-temporal-alignment-repair.md) 与 E-010 的 5-epoch 全量试跑 | 用户明确确认后才能启动新的 500-epoch 正式实验；完成后再重跑同步和后处理评估 |

## 状态约定

- `候选，未冻结`：可以继续验证，不能写成默认实现或论文最终结论。
- `已冻结`：当前流程必须遵守；变更时新增决策或明确更新本条并附证据。
- `已废弃`：保留历史原因，但不再用于当前实现或正文。
