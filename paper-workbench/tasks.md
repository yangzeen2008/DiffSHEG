# 论文任务看板

> 规则：同一时间最多保留 3 个“进行中”任务。任务完成后勾选，并在需要时把证据链接回主张或实验台账。

## 进行中 / 下一步

- [ ] `P0` 用户确认后启动 `beat_FM_aa_x0_aligned_sync_v1` 的正式 500-epoch 重训；启动时回读并核验时间轴 manifest、运动 cache ID 与 HuBERT 绑定。
- [ ] `P0` 新模型完成后重新生成 15 秒以上 GT/预测/旧模型视觉对比，评估表情—语音同步并替换旧结论。
- [ ] `P0` 确认学校论文模板、提交截止日期、目标字数和引用格式。
- [ ] `P0` 统一实验数字口径：重点核对 1-Step 的 `33.3x` 与 `1100x` 两种加速表述所使用的基线和计时单位。
- [ ] `P0` 建立完整参考文献库，并为绪论中的每个外部研究结论补上可追踪引用。
- [ ] `P0` 对单向表情—手势联合 FM 做系统查新，逐页核对 GlobalDiff、GestureLSM、SemConFlow 和 Match-TTSG，冻结可使用的创新表述。

## 正文

- [ ] `P1` 完成第二章：DDPM、DiffSHEG、Flow Matching、OT-CFM 与旋转表示基础。
- [ ] `P1` 完成第三章：模型架构、损失函数、ODE 求解、长序列生成和后处理。
- [ ] `P1` 完成第四章：实验设置、主对比、消融、效率、定性结果与局限性。
- [ ] `P1` 完成第五章：总结、局限和未来工作。
- [ ] `P1` 在全文完成后回写摘要、创新点和章节组织，保证术语与数字一致。

## 证据与质量

- [ ] `P0` 对预检筛选后的 `blend=5/7` 运行全验证集关节级 SO(3) 与 51 维表情统计，并据此冻结默认推理参数。
- [ ] `P1` 对 `blend=5` 与 `blend=7` 的手势和表情做去标签盲评；若肉眼无稳定差异，不在正文夸大视觉提升。
- [ ] `P1` 补齐原论文口径的 FMD/FED 与音唇同步评估；当前仓库缺少原作者面部特征提取器，不得用系数空间指标冒充。
- [ ] `P1` 为 FGD、PCK、MSE、Diversity、SRGR、FPS 明确公式、实现位置和统计口径。
- [ ] `P1` 将早期错误关节顺序的实验与最终有效实验在正文引用层面彻底隔离。
- [ ] `P1` 为所有速度结论保存硬件、batch/clip、帧数、warm-up、重复次数和原始日志。
- [ ] `P1` 完成单向联合 FM 消融：无方向、无表情条件、`E_t/v_E/E_0_hat`、有无 detach、反向/双向条件和真实表情上界。
- [ ] `P2` 为全部论文图表确定图号、标题、正文引用位置和一句话结论。
- [ ] `P2` 完成术语、单位、符号、图表编号、交叉引用和参考文献终检。

## 已完成

- [x] 初始化论文一站式工作台与自动体检脚本。
- [x] 接入现有论文草稿、路线图、实验结果、项目进展和图表目录。
- [x] 完成 `beat_FM_aa_x0_aligned_v1` 的 500 epochs 训练，下载 Epoch 479 的 `pck_best.tar`。
- [x] 完成 quiet/typical/energetic 三段长序列 SO(3) 修复验证和 `blend=0/3/5/7` 消融。
- [x] 生成五路同步对比、双路边界 4 倍慢放和前臂逐帧角速度证据。
- [x] 修复 120 FPS 动作、60 FPS 表情与 16 kHz 音频的跨模态时间轴，完成全量源数据、运动 LMDB v3 与 HuBERT v3 审计。
- [x] 完成 `beat_FM_aa_x0_aligned_sync_pilot_v1` 的 2-batch 冒烟和 5-epoch 全数据准入试跑；确认 checkpoint 绑定、有限值和训练收敛趋势。
